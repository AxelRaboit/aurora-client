{
    "private": true,
    "license": "UNLICENSED",
    "description": "Shared runtime deps for client Vue components. Aurora's vite.config.js dedupes these so we still get a single instance at runtime.",
    "scripts": {
        "test": "vendor/axelraboit/aurora/node_modules/.bin/vitest run --config vitest.config.js"
    },
    "devDependencies": {
        "vitest": "^4.1.11"
    },
    "dependencies": {
        "axios": "^1.20.0",
        "lucide-vue-next": "^1.0.0",
        "vue": "^3.5.42",
        "vue-i18n": "^11.4.10",
        "vue-sonner": "^2.0.9"
    }
}
