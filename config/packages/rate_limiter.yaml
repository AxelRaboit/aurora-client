framework:
    rate_limiter:
        # **Les limiteurs d'aurora-core arrivent avec le paquet** depuis la
        # 0.9.212. Ses contrôleurs en câblent sept par leur nom, et les
        # répéter ici était au projet, sans que rien ne le dise : un conteneur
        # qui refuse de se construire au premier déploiement, sur un service
        # dont ce projet n'a jamais entendu parler.
        #
        # Ce fichier ne garde donc que ce qui appartient à ce projet, ou ce
        # qu'il veut chiffrer autrement : sa configuration est chargée après
        # celle du paquet, donc une clé redéclarée ici gagne.
